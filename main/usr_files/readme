##Possible parameter to be changed in usr files

#userchk
comment or uncomment

call estimate_strouhal
call comp_vort3(vort , w1, w2, vx, vy, vz)
call copy (T,vort,n)    ! Vorticity --> T
ifto = .true.           ! Dump vorticity as T

#userbc
n          !number of Re changes
dtrans     !transient time interval
t_trans(1) !time point that start to decrease Re
pert(0)    !initial disturbance amplitude
vel(1)     !Re factor that one want to decrease from
a4         !asymmetry

#usrdat2
aa     = 1         !major axis (in x direction)
bb     = 1         !minor axis (in y direction)
alpha  = 0*pi/180  !attack angle

